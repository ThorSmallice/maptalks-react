.maptalks-map-root {
    @apply w-full h-full relative;
}
.maptalks-map {
    @apply w-full h-full absolute top-0 left-0;
}
.maptalks-map-children {
    @apply w-full h-full absolute top-0 left-0 pointer-events-none overflow-hidden;
}
